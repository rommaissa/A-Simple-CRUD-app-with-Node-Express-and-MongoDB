<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Building a Simple CRUD app with Node, Express, and MongoDB</title>
    <link rel="stylesheet" href="style/global.css">
</head>

<body>
    <div class="container">
        <h1 class="title">A Simple CRUD app with Node, Express, and MongoDB</h1>
        <section class="add-quote">
            <h2>Add Quote</h2>
            <form class="form" action="/quotes" method="POST">
                <input type="text" placeholder="name" name="name" autocomplete="off">
                <input type="text" placeholder="quote" name="quote" autocomplete="off">
                <button type="submit" id="submit">Submit</button>
            </form>
        </section>
        <section class="update-quote">
            <h2>Darth Vadar invades!</h2>
            <p>Replace first Yoda's quote with a quote written by Darth Vadar</p>
            <button id="update-button">Replace Yoda's quote </button>
        </section>
        <section class="delete-quote">
            <h2>Remove Darth Vadar!</h2>
            <p>Delete one Darth Vadar's quote. Does nothing if there are no more Darth Vadar's quote</p>
            <button id="delete-button">Delete</button>
            <div id="message"></div>
        </section>
        <section class="display-quote">
            <h1>Quotes :</h1>
            <ul class="quotes">
                <% if (quote) { %>
                <% for (var i= 0; i < quote.length; i++) {%>
                <li>
                    <span><%= quote[i].name %></span>:

                    <span> <%= quote[i].quote %></span>
                </li>
                <%}%>
            <% } %>
            </ul>
        </section>
    </div>

    <script src="app.js"></script>
</body>

</html>